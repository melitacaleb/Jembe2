#!/bin/sh
# Gradle wrapper for Farmers Connect
set -e
DIRNAME=$(dirname "$0")
WRAPPER_JAR="$DIRNAME/gradle/wrapper/gradle-wrapper.jar"
JAVACMD="${JAVA_HOME:+$JAVA_HOME/bin/}java"
exec "$JAVACMD" -classpath "$WRAPPER_JAR" org.gradle.wrapper.GradleWrapperMain "$@"
